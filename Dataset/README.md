# Dataset Folder

This folder is intentionally kept lightweight in GitHub.

Local generated datasets are ignored by `.gitignore`, including:

```text
image_capture/
labeledandimg/
shirt_crop_dataset/
shirt_cls_dataset/
counting_output/
```

Regenerate them with the scripts in `scripts/` and `run_scripts/`.
